replicaCount: 2

image:
  repository: youracr.azurecr.io/digital-banking-frontend
  tag: "latest"
  pullPolicy: IfNotPresent

containerPort: 8080

service:
  type: ClusterIP
  port: 80
  targetPort: 8080

ingress:
  enabled: true
  className: nginx
  host: aks-mvp-dbg.yourdomain.com
  path: /
  pathType: Prefix

resources: {}
